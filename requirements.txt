# RAID dependencies — pinned to the versions verified by backend/verify.py.
# Railway (nixpacks) installs from this root file; the Procfile runs backend/worker.py.
supabase==2.31.0
anthropic==0.109.1
python-dotenv==1.2.2
httpx==0.28.1
resend==2.30.1
tzdata==2026.2
